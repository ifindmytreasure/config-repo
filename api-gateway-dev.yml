zuul:
  routes:
    myProduct:
      path: /myProduct/**
      serviceId: product
      sensitiveHeaders:
  ignored-patterns:
    - /**/product/listfororder #限制访问
management:
  endpoints:
    web:
      exposure:
        include: "*"
